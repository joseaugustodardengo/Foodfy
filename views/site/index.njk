{% extends "site/layout.njk" %}

{% block content %}

    <main>
        <section class="banner">
            <div class="container">
                <div>
                    <h1>As melhores receitas</h1>
                    <p>Aprenda a construir os melhores pratos
                        com receitas criadas por profissionais
                        do mundo inteiro.</p>
                </div>
                <img src="/chef.png" alt="" class="banner-image">
            </div>
        </section>
    
        <section class="more-access">
            <div class="container">
                <h2>Mais acessadas</h2>
                <div class="cards">
                    
                    {% for item in items.slice(0, 6) %}                    
                        <div class="recipe-card">
                            <img src="{{item.image}}" alt="Imagem de {{item.image}}" class="image">
                            <h3 class="title">{{item.title}}</h3>
                            <p class="subtitle">{{item.author}}</p>                    
                        </div>
                    {% endfor %}                                     
                    
                </div>
            </div>
        </section>
    </main>      

{% endblock %}